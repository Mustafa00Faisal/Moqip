# MOAQIB 6.1.0 — ملاحظات مرحلة الإصلاح الكبرى الثانية

هذه الحزمة تكمل إصلاح 6.0.1 مع الحفاظ على واجهة V6 ومسارات العمل الأساسية. تركيزها هو منع فقدان البيانات عند العمل من أكثر من جهاز، وإضافة حوكمة قابلة للمراجعة للمعاملات والدفعات.

## النتيجة المختصرة

- منع الكتابة الصامتة فوق Snapshot أحدث بواسطة مراجعة `revision` ومقارنة ذرية قبل كل تحديث.
- إضافة نافذة واضحة لحل تعارض النسخة المحلية والسحابية، مع خيار تصدير نسخة الجهاز قبل القرار.
- إضافة حارس PostgreSQL يرفض أي إدخال لا يبدأ بالمراجعة 1 وأي تحديث لا يزيد المراجعة بمقدار واحد.
- تحويل حذف المعاملة إلى حذف ناعم قابل للاستعادة من مركز البيانات.
- إضافة سجل تدقيق تشغيلي تراكمي للعمليات الحساسة.
- إضافة عكس الدفعة بدلاً من حذفها، مع سبب وتاريخ وحفظ الدليل المالي.
- تثبيت مرجع سند القبض لكل دفعة بحيث لا يتغير عند إعادة الطباعة.
- إضافة «إعادة فتح المعاملة» للمعاملات المنجزة.
- توسيع فحص صحة البيانات ليشمل الكيانات والدفعات والمراجع والمحذوفات وسجل التدقيق.
- إزالة فروع Capacitor/Firebase القديمة والإبقاء على Web/PWA وWeb Push فقط.
- تثبيت إصدارات Supabase JS وChart.js بدلاً من الروابط العائمة.

## الأخطاء الإضافية التي ظهرت أثناء التدقيق

1. الواجهة كانت تطلب من المستخدم إعادة فتح المعاملة، لكن الدالة المطلوبة لم تكن موجودة.
2. رقم سند القبض كان يُنشأ وقت الطباعة، لذلك كانت الدفعة نفسها تحصل على رقم مختلف في كل مرة.
3. رابط Supabase كان يستخدم الإصدار العائم `@2`، ورابط Chart.js كان عائماً أيضاً.
4. حالة تعارض المزامنة لم تكن مميزة عن مجرد تغييرات تنتظر الرفع.
5. فحص صحة البيانات لم يكن يكشف تكرار معرفات العملاء والشركات والدفعات أو نقص دليل العكس.
6. سياسة RLS قد تبقى غير آمنة إذا وُجدت سياسة قديمة متساهلة بجانب السياسات الجديدة؛ ملف SQL الآن يستبدل كل سياسات `app_data` بسياسات المالك الأربع فقط.
7. نافذة قديمة من 6.0.1 كان يمكنها تجاوز فحص المراجعة الموجود في العميل الجديد؛ أضيف Trigger على الخادم لمنع ذلك.
8. تصدير النسخة الاحتياطية كان يحسب checksum ثم يعيد قراءة `db` بعد عملية غير متزامنة، ما يسمح نظرياً باختلاف البصمة عن المحتوى. أصبح التصدير يعتمد Snapshot واحدة مجمدة.
9. مولد مرجع السند القديم لم يضمن تمييز بعض المعرفات التراثية غير اللاتينية؛ أضيف مرجع حتمي ثابت لها.

## عقد البيانات الجديد

بقي `schemaVersion` عند 6 حفاظاً على توافق V5/V6، وأضيفت حقول حوكمة مستقلة:

- `governanceVersion: 1`
- `paymentLedgerVersion: 2`
- `trash.transactions[]`
- `auditLog[]`
- لكل دفعة: `status`, `receiptRef`, `reversedAt`, `reversalReason`

التطبيع يحافظ على الحقول القديمة، ويحوّل الدفعات القديمة إلى `posted`، ويولد لها مراجع ثابتة، ويستبعد الدفعات `reversed` من المدفوع والتحصيل والتقارير واتجاه السيولة.

## المزامنة وحل التعارض

كل صف في `public.app_data` يملك مراجعة رقمية. التحديث ينجح فقط عندما تكون مراجعة الخادم مساوية للمراجعة التي قرأها الجهاز. إذا تغيّرت:

1. يتوقف الرفع ولا تُستبدل أي نسخة.
2. تظهر نافذة تعرض مراجعة الجهاز والمراجعة السحابية.
3. يستطيع المستخدم تصدير نسخة الجهاز.
4. يمكن اعتماد السحابة أو رفع نسخة الجهاز فوق أحدث مراجعة بعد قرار صريح.
5. قرار حل التعارض نفسه يُضاف إلى سجل التدقيق.

هذه الآلية تمنع فقدان Snapshot كاملة، لكنها ليست دمجاً حقلياً تلقائياً؛ عند التعارض يختار المستخدم إحدى النسختين.

## سجل التدقيق

يسجل التطبيق أهم عمليات:

- إنشاء/حذف العملاء والشركات.
- إنشاء المعاملة، إكمالها، إعادة فتحها، حذفها واستعادتها.
- المحطات، التلكؤ، الملاحظات، المتابعات والأولوية.
- إنشاء الدفعة وعكسها.
- إنشاء/حذف مستند الخزنة.
- الاستيراد والإصلاح وتغيير التوقيع وحل تعارض السحابة.

كل قيد يتضمن الوقت، نوع الإجراء، نوع ومعرف الكيان، المستخدم، معرف الجهاز والتفاصيل المناسبة. السجل تراكمي ولا توجد واجهة لحذف قيد منفرد، لكنه ما زال جزءاً من Snapshot يملكها المستخدم؛ لذلك ليس سجلاً محاسبياً غير قابل للتلاعب على مستوى خادم مستقل.

## ترتيب النشر الآمن — إلزامي

1. صدّر نسخة JSON من التطبيق الحالي وخذ Backup لقاعدة Supabase.
2. اختبر `SUPABASE_SECURITY_SETUP.sql` أولاً على مشروع Staging مطابق.
3. استخدم نافذة صيانة قصيرة، ثم شغّل ملف SQL يدوياً في Supabase SQL Editor.
4. تحقق من RLS والسياسات والـTrigger باستعلامات التحقق الموجودة أسفل الملف.
5. انشر ملفات 6.1.0 فوراً على HTTPS.
6. أغلق كل نوافذ 6.0.1 القديمة وأعد فتح التطبيق. سيمنع Trigger كتابتها القديمة، لكنه لا يستطيع تحديث JavaScript المفتوح داخلها.
7. اختبر بحساب تجريبي: دخول، إنشاء معاملة، دفعة، عكس، حذف/استعادة، Offline/Online، ثم تعارض متعمد بين جهازين.

إذا لم يُشغّل ملف SQL أولاً، سيوقف التطبيق الدخول برسالة ترقية قاعدة البيانات بدلاً من إظهار خطأ اتصال مضلل.

## التراجع

لا يكفي استبدال ملفات 6.1.0 بملفات 6.0.1 بعد بدء استخدام عكس الدفعات؛ الإصدار الأقدم لا يفهم استبعاد `reversed` من الإجماليات. التراجع الآمن يعني استعادة نسخة البيانات وقاعدة البيانات المأخوذة قبل نشر 6.1.0، أو إصلاح إصدار 6.1.0 بدلاً من الرجوع بالكود وحده.

## التحقق المنفذ

- نتيجة مدقق الإصدار: **64/64** فحصاً ناجحاً.
- **13** حزمة Smoke تغطي الإنشاء، الكيانات، المعاملات، الأرشيف، الخزنة، المالية، التحليلات، البحث، الإقلاع، السحابة، إصلاح 6.0.1 وإصلاح 6.1.0.
- حزمة 6.1.0 تضيف 12 سيناريو خاصاً بالحوكمة، rollback، التعارض، revision، الحذف والاستعادة، عكس الدفعة، المراجع الثابتة وSQL.
- فحص JavaScript لكل ملفات Runtime، ومعرفات HTML، handlers، المراجع المحلية، بنية CSS، Manifest، Service Worker، RLS والعقود الجديدة.

## ما لم يُنفذ أو يُختبر حياً

- لم يُطبق أي تغيير على مشروع Supabase الإنتاجي.
- لم يتوفر Chromium/Playwright في بيئة العمل، لذلك لم يُنفذ E2E بصري حقيقي أو طباعة PDF فعلية من متصفح.
- لم يُنفذ اختبار متزامن فعلي على جهازين ضد قاعدة Staging؛ تمت محاكاة مسارات النجاح والتعارض والفشل آلياً.
- صور الخزنة ما زالت Base64 داخل Snapshot/localStorage. نقلها إلى Supabase Storage/IndexedDB هو المرحلة التالية المقترحة.
- المكتبات الحرجة أصبحت مثبتة الإصدار، لكنها لا تزال من CDN؛ التوريد المحلي مع CSP صارمة هو مرحلة لاحقة.
- Web Push اختياري ومعطل افتراضياً حتى إعداد VAPID والجداول/الوظيفة الخلفية الخاصة به.
- سجل التدقيق داخل Snapshot وليس جدول Audit مستقل غير قابل للتعديل.

## ملفات التسليم المهمة

- `index.html`, `app.js`, `productivity.js`, وملفات CSS: Runtime 6.1.0.
- `SUPABASE_SECURITY_SETUP.sql`: ترقية Supabase اليدوية المطلوبة.
- `remediation_phase2_smoke.js`: اختبارات المرحلة الثانية.
- `remediation_release_audit.py`: مدقق الحزمة الكامل.
- `remediation_release_audit_result.json`: النتيجة والبصمات SHA-256 لملفات Runtime.

## المرحلة التالية المقترحة

ترحيل الخزنة من Base64/localStorage إلى Supabase Storage مع metadata في Postgres ونسخة Offline عبر IndexedDB، ثم توريد المكتبات محلياً وفرض CSP، ثم تشغيل E2E بصري ومتعدد الأجهزة على Staging قبل أي تحديث مميزات كبير.
