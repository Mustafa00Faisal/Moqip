# MOAQIB V7 — المرحلة التأسيسية 01

الإصدار: `7.0.0-foundation.1`

الحالة: **نسخة تطويرية مستقلة للاختبار، وليست بديلًا إنتاجيًا عن 6.1.0 بعد.**

## ما أُنجز في هذه المرحلة

### 1. نواة انتقالات V7

- إزالة الفاصل الزمني الذي كان يترك إطارًا فارغًا صغيرًا عند تبديل الصفحات.
- إضافة انتقالات متجهة بين الواجهات باستخدام `opacity` و`transform` فقط.
- استخدام View Transition API عندما يدعمها Chrome، مع مسار بديل آمن للأجهزة الأخرى.
- تحسين الانتقال بين أقسام مركز التحكم وبين السجل والخزنة.
- تحسين فتح وإغلاق النوافذ دون الاعتماد على تأخير ثابت لإظهار المحتوى.
- احترام إعداد النظام «تقليل الحركة» بالكامل.

### 2. حركة تكيفية حسب الجهاز

تختار V7 واحدًا من ثلاثة أوضاع تلقائيًا:

- `full`: الحركة الكاملة على الجهاز المناسب.
- `lite`: مدد أقصر وإلغاء المؤثرات الثقيلة عند تفعيل توفير البيانات أو انخفاض موارد الجهاز.
- `none`: تعطيل الحركة عند طلب المستخدم تقليل الحركة من إعدادات النظام.

### 3. تحسينات الأداء

- منع تحميل الرسم البياني أثناء ظهور بوابة تسجيل الدخول.
- منع إعادة رسم التطبيق الفارغ عدة مرات أثناء فحص جلسة Supabase أو تسجيل الخروج.
- استخدام لقطة مركز العمل اليومية المخزنة مؤقتًا بدل إعادة حسابها في كل رسم.
- تحويل حساب أعداد معاملات المحامين والشركات والمستندات إلى مرور خطي واحد على البيانات.
- تقييد زمن تأخير دخول البطاقات حتى لا تنتظر البطاقات الأخيرة طويلًا في القوائم الكبيرة.
- إضافة `content-visibility` و`contain` للبطاقات الطويلة لتقليل كلفة الرسم خارج الشاشة.
- تعطيل تمويه الخلفية المكلف داخل القوائم والنوافذ على الهاتف، مع الحفاظ على نفس عمق التصميم اللوني.
- إضافة قياسات أداء محلية يمكن قراءتها من `getMoaqibV7PerformanceSnapshot()`؛ لا تُرسل هذه القياسات إلى أي خادم.

اختبار الأداء الاصطناعي مرّر:

- 240 محاميًا.
- 720 شركة.
- 6000 معاملة.
- 1600 مستند.
- قراءة واحدة فقط لمجموعة المعاملات عند بناء دليل العمل.

الزمن المسجل في بيئة الاختبار كان قرابة 15ms، لكنه ليس وعدًا بزمن مماثل على كل هاتف.

كما اجتازت الحزمة `78/78` من فحوص الإصدار، ثم نجحت جميع اختبارات الرجوع الوظيفية الخاصة بـ6.1 واختبارات V7 الجديدة بصورة مستقلة.

### 4. أساس ترحيل البيانات

- أُضيف عقد غير تنفيذي في `v7/schema-contract.json` يحدد جداول V7 والعلاقات والأدوار والفهارس المطلوبة.
- أُضيفت أداة `v7/migration-preview.js` لتحويل نسخة V6 في الذاكرة إلى خطة جداول V7.
- الأداة لا تتصل بالإنترنت ولا تكتب في `localStorage` أو Supabase.
- تتحقق من تكرار المعرّفات، العلاقات اليتيمة، المعاملات المحذوفة، الدفعات المعكوسة، المستندات وسجل التدقيق.
- حافظ التصميم على المعرّفات النصية الحالية كي تبقى نسخ V5/V6 قابلة للترحيل دون إعادة ترقيم مفقِدة للمراجع.

لم يُنشأ أو يُنفذ SQL جديد في هذه المرحلة. ملف `SUPABASE_SECURITY_SETUP.sql` الموجود هو ملف 6.1.0 الذي شُغّل سابقًا فقط.

## طريقة تجربة النسخة بأمان

1. احتفظ بحزمة 6.1.0 الحالية كما هي ولا تحذفها.
2. صدّر نسخة احتياطية من داخل التطبيق قبل التجربة.
3. انشر V7 في مسار أو بيئة اختبار منفصلة أولًا، مع المحافظة على مجلد `v7` ومجلد `icons` وبقية الملفات كما هي.
4. لا تشغّل أي SQL إضافي لهذه المرحلة؛ قاعدة البيانات الحالية المتوافقة مع 6.1 تكفي.
5. بعد رفع الملفات، أغلق التطبيق المثبت وافتحه مرتين أو حدّث الصفحة تحديثًا كاملًا حتى يتسلّم Service Worker الجديد ذاكرة التخزين `moaqib-v7-foundation-01`.
6. اختبر الدخول، إنشاء معاملة، الدفعات، الإيقاف والاستئناف، الأرشيف، سلة المحذوفات، النسخ الاحتياطي والاستعادة، ثم افحص الحركة على هاتف متوسط أو ضعيف.
7. إذا ظهر خلل يمنع العمل، ارجع فورًا إلى 6.1.0؛ بنية البيانات لم تُرقَّ بعد، لذلك مسار الرجوع ما زال مباشرًا في هذه المرحلة.

## ما بقي من V7

1. بناء جداول Supabase الجديدة وسياسات RLS في بيئة مرحلية.
2. ترحيل الخزنة إلى Supabase Storage وIndexedDB.
3. تعدد المستخدمين والأدوار والصلاحيات وسجل تدقيق خادمي.
4. OCR مع شاشة مراجعة قبل اعتماد البيانات.
5. المساعد الذكي والبحث الدلالي ومركز العمل المدعوم بالذكاء الاصطناعي.
6. التنبيهات المجدولة والأتمتة.
7. التحليلات والتوقعات الجديدة.
8. اختبار متصفح حقيقي، جهازين، Offline، تعارضات وصلاحيات قبل الإصدار الإنتاجي.

## حدود التحقق الحالي

- لم يُنفذ أي تغيير على مشروع Supabase الحي.
- لم يُجر اختبار بصري فعلي داخل Chrome/Android في بيئة العمل الحالية.
- لم تُضف AI أو OCR أو الجداول الإنتاجية بعد؛ هذه مرحلة الأساس والأداء والحركة.
- تبقى 6.1.0 نسخة الرجوع الإنتاجية حتى اكتمال بوابات اختبار V7.

## مراجع Supabase المعتمدة للتصميم القادم

- حماية كل جدول مكشوف عبر RLS وفهرسة أعمدة السياسات: https://supabase.com/docs/guides/database/postgres/row-level-security
- الحاويات الخاصة في Storage خاضعة لسياسات RLS: https://supabase.com/docs/guides/storage/buckets/fundamentals
- الجداول الجديدة قد تحتاج منح صلاحيات صريحًا كي تظهر في Data API: https://supabase.com/changelog/45329-breaking-change-tables-not-exposed-to-data-and-graphql-api-automatically
- صلاحيات قنوات Realtime الخاصة تعتمد على RLS: https://supabase.com/docs/guides/realtime/authorization
